const postReducer = (state, action) => {
  switch (action.type) {
    case "TYPE_1":
      return state

    case "TYPE_2":
      return state

    default:
      return state
  }
}
export default postReducer
