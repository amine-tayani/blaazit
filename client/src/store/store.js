// this is the global store for state management

export const userInitialState = {
  status: "offline",
  authToken: null,
  user: null,
  loading: false,
  userError: null,
  isAuthenticated: false,
}
